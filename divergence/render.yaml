services:
  - type: web
    name: divergence
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python divergence_cloud.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: WEBHOOK_URL
        sync: false
